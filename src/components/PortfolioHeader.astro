---
interface Profile {
  network: string;
  url: string;
}

interface Props {
  name: string;
  label: string;
  headline: string;
  tagline: string;
  email: string;
  availability: string;
  currentFocus: string;
  profiles: Profile[];
}

const { name, label, headline, tagline, email, availability, currentFocus, profiles } = Astro.props;

const linkedIn = profiles.find(p => p.network === 'LinkedIn');
const github = profiles.find(p => p.network === 'GitHub');
---

<div class="grid grid-cols-1 md:grid-cols-12 gap-6 mb-6">
  <!-- Hero Section -->
  <header class="bg-white dark:bg-slate-900/60 backdrop-blur-xl border border-gray-900 dark:border-slate-700/50 rounded-3xl transition-all hover:border-indigo-500/30 md:col-span-8 p-8 md:p-12 flex flex-col justify-between group relative overflow-hidden" aria-labelledby="hero-name">
    <div class="absolute inset-0 opacity-5 group-hover:opacity-10 transition-opacity pointer-events-none">
      <svg class="w-full h-full" viewBox="0 0 100 100" preserveAspectRatio="none">
        <defs>
          <pattern id="grid" width="10" height="10" patternUnits="userSpaceOnUse">
            <path d="M 10 0 L 0 0 0 10" fill="none" stroke="currentColor" stroke-width="0.5"/>
          </pattern>
        </defs>
        <rect width="100" height="100" fill="url(#grid)"/>
      </svg>
    </div>
    <div class="relative z-10">
      <div class="flex items-center gap-4 mb-6">
        <img
          src="/profile.png"
          alt={name}
          class="w-16 h-16 rounded-2xl object-cover shadow-lg ring-2 ring-indigo-500/20"
        />
        <div>
          <h1 id="hero-name" class="text-5xl md:text-7xl font-extrabold text-gray-900 dark:text-white tracking-tighter">{name}</h1>
        </div>
      </div>
      <div class="space-y-4 max-w-2xl">
        <p class="text-base md:text-lg text-gray-600 dark:text-slate-400 leading-relaxed">
          Principal engineer with <span class="text-gray-900 dark:text-white font-medium">14+ years</span> building digital products at
          <span class="text-gray-900 dark:text-white font-medium">FTSE 100 enterprises</span> and
          <span class="text-gray-900 dark:text-white font-medium">early-stage startups</span>.
          Specialized in <span class="text-indigo-600 dark:text-indigo-400">React</span>, <span class="text-indigo-600 dark:text-indigo-400">AWS</span>, and
          <span class="text-indigo-600 dark:text-indigo-400">Identity/Auth systems</span> —
          architecting platforms that serve <span class="text-gray-900 dark:text-white font-medium">millions of daily users</span> at
          Tesco, Chase UK, and Cisco.
        </p>
        <p class="text-sm md:text-base text-gray-500 dark:text-slate-500">
          Recently focused on integrating <span class="text-gray-700 dark:text-slate-400">GenAI</span> (Claude, Amazon Bedrock) into enterprise workflows,
          delivering <span class="text-gray-700 dark:text-slate-400">40% performance improvements</span> and
          <span class="text-gray-700 dark:text-slate-400">60% security gains</span> through modern cloud architecture.
        </p>
      </div>
    </div>

    <div class="relative z-10 mt-12 flex flex-wrap gap-6 items-center border-t border-gray-200 dark:border-slate-800 pt-8">
      <div class="flex items-center gap-3">
        <div class="relative">
          <div class="w-3 h-3 bg-green-500 rounded-full animate-ping absolute opacity-75"></div>
          <div class="w-3 h-3 bg-green-500 rounded-full"></div>
        </div>
        <span class="font-mono text-xs text-gray-600 dark:text-slate-400">{availability}</span>
      </div>
      <nav class="flex gap-4" aria-label="Social links">
        {github && (
          <a href={github.url} target="_blank" rel="noopener noreferrer" class="text-gray-600 dark:text-slate-400 hover:text-gray-900 dark:hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 rounded" aria-label="GitHub profile">
            <span class="material-symbols-outlined" aria-hidden="true">code</span>
          </a>
        )}
        {linkedIn && (
          <a href={linkedIn.url} target="_blank" rel="noopener noreferrer" class="text-gray-600 dark:text-slate-400 hover:text-gray-900 dark:hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 rounded" aria-label="LinkedIn profile">
            <span class="material-symbols-outlined" aria-hidden="true">person</span>
          </a>
        )}
        <a href={`mailto:${email}`} class="text-gray-600 dark:text-slate-400 hover:text-gray-900 dark:hover:text-white transition-colors focus:outline-none focus:ring-2 focus:ring-indigo-500 rounded" aria-label="Send email">
          <span class="material-symbols-outlined" aria-hidden="true">mail</span>
        </a>
      </nav>
    </div>
  </header>

  <!-- Status Card -->
  <section class="bg-white dark:bg-slate-900/60 backdrop-blur-xl border border-gray-900 dark:border-slate-700/50 rounded-3xl transition-all hover:border-indigo-500/30 md:col-span-4 p-8 flex flex-col justify-between dark:bg-gradient-to-b dark:from-slate-900 dark:to-slate-950" aria-labelledby="status-heading">
    <div class="space-y-6">
      <div class="space-y-4">
        <div class="border-b border-gray-200 dark:border-slate-800 pb-3">
          <p class="text-[10px] font-mono text-gray-500 dark:text-slate-500 uppercase mb-2">Tech Stack</p>
          <div class="space-y-1">
            <p class="text-gray-900 dark:text-white text-sm font-medium">React • AWS • TypeScript</p>
            <p class="text-gray-600 dark:text-slate-400 text-xs">WebAuthn • OAuth2 • Bedrock</p>
          </div>
        </div>

        <div class="border-b border-gray-200 dark:border-slate-800 pb-3">
          <p class="text-[10px] font-mono text-gray-500 dark:text-slate-500 uppercase mb-2">Recent Companies</p>
          <div class="flex flex-wrap gap-1">
          <span class="text-xs bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-slate-300 px-2 py-0.5 rounded">CaspianOne Open banking</span>
          <span class="text-xs bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-slate-300 px-2 py-0.5 rounded">Climatise</span>
            <span class="text-xs bg-gray-100 dark:bg-slate-800 text-gray-700 dark:text-slate-300 px-2 py-0.5 rounded">Tesco</span>
          </div>
        </div>

        <div class="border-b border-gray-200 dark:border-slate-800 pb-3">
          <p class="text-[10px] font-mono text-gray-500 dark:text-slate-500 uppercase mb-2">Outside of Work</p>
          <ul class="space-y-1.5">
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-2">
              <span class="material-symbols-outlined text-sm text-amber-600 dark:text-amber-400">handyman</span>
              <span>DIY woodworking & carpentry</span>
            </li>
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-2">
              <span class="material-symbols-outlined text-sm text-blue-600 dark:text-blue-400">home_app_logo</span>
              <span>Home automation systems</span>
            </li>
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-2">
              <span class="material-symbols-outlined text-sm text-purple-600 dark:text-purple-400">view_in_ar</span>
              <span>3D printing & CAD modeling</span>
            </li>
          </ul>
        </div>

        <div>
          <p class="text-[10px] font-mono text-gray-500 dark:text-slate-500 uppercase mb-2">Recent Impact</p>
          <ul class="space-y-1">
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-1">
              <span class="text-green-600 dark:text-green-400">→</span>
              <span>GenAI platform with Claude</span>
            </li>
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-1">
              <span class="text-green-600 dark:text-green-400">→</span>
              <span>40% Lambda optimization</span>
            </li>
            <li class="text-xs text-gray-600 dark:text-slate-400 flex items-start gap-1">
              <span class="text-green-600 dark:text-green-400">→</span>
              <span>60% security improvement</span>
            </li>
          </ul>
        </div>
      </div>
    </div>

    <a href={`mailto:${email}`} class="w-full group relative inline-flex items-center justify-center p-0.5 overflow-hidden text-sm font-medium text-white rounded-xl bg-gradient-to-br from-indigo-500 to-purple-500 hover:text-white focus:ring-4 focus:outline-none focus:ring-indigo-500 focus:ring-offset-2 transition-all" aria-label="Get in touch via email">
      <span class="relative w-full px-5 py-3 transition-all ease-in duration-75 bg-white dark:bg-slate-950 rounded-lg group-hover:bg-opacity-0 text-center text-gray-900 dark:text-white group-hover:text-white">
        Get In Touch
      </span>
    </a>
  </section>
</div>
